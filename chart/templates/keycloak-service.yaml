{{- if .Values.keycloak.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "katta-server.fullname" . }}-service-kc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "katta-server.labels" . | nindent 4 }}
    app.kubernetes.io/component: keycloak
spec:
  type: {{ .Values.keycloak.service.type }}
  selector:
    {{- include "katta-server.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: keycloak
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.keycloak.service.httpPort }}
      targetPort: http
    - name: mgmt
      protocol: TCP
      port: {{ .Values.keycloak.service.managementPort }}
      targetPort: management
{{- end }}
