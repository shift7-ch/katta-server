apiVersion: v1
kind: Secret
metadata:
  name: {{ include "katta-server.fullname" . }}-secrets-kc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "katta-server.labels" . | nindent 4 }}
  {{- if .Values.secrets.keepOnUninstall }}
  annotations:
    helm.sh/resource-policy: keep
  {{- end }}
type: Opaque
stringData:
  {{- if .Values.keycloak.enabled }}
  kc_admin_password: {{ include "katta-server.resolvedKeycloakAdminPassword" . | quote }}
  kc_db_password: {{ include "katta-server.resolvedKeycloakDbPassword" . | quote }}
  {{- end }}
  realm.json: |
{{ include "katta-server.realmJson" . | indent 4 }}